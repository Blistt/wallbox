This is a content-based image retrieval system that takes an image as a query and outputs a set of similar images. This project uses pre-trained deep learning embeddings (VGG-19) to calculate image-to-image similarity. A next js framework is used for the front end and flask for the backend